# Skin-Disease-Detection-using-histogram
This project detects skin diseases using color histogram features extracted from HSV images. The histogram generates 512 features per image, which are classified using an SVM model. The system evaluates performance using accuracy and confusion matrix.
